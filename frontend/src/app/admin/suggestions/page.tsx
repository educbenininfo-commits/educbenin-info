// Écran back-office Suggestions — 12-backoffice-suggestions.md. Renamed
// from "Signalements" (the word never appears here — CLAUDE.md "Extension
// multi-écoles"): "Suggérer" côté public, "Suggestions" côté équipe.

import { SuggestionsList } from '@/components/backoffice/suggestions/SuggestionsList';

export default function SuggestionsPage() {
  return <SuggestionsList />;
}
